Steps to run the code:

**For backend**:

run the commands:

python3 -m venv .venv

.venv\Scripts\activate

pip install -r requirements.txt

flask --app hello run

**For frontend**:

run the commands:

npm install 

npm start
